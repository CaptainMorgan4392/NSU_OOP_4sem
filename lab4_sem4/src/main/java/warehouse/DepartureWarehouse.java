package warehouse;

import production.ProductType;

public class DepartureWarehouse extends Warehouse {
    public DepartureWarehouse(ProductType productType, int capacity) {
        super(productType, capacity);
    }
}
